<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="viewHolder"
            type="com.univ_amu.localizcci.ui.SpotListAdapter.ViewHolder" />
    </data>


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="5dp"
        android:paddingTop="3dp"
        android:background="@android:color/holo_orange_light"
        android:padding="15dp"
        android:onClick="@{viewHolder::onClick}">


        <ImageView
            android:id="@+id/imgspot"
            android:layout_width="200dp"
            android:layout_height="120dp"
            android:layout_marginEnd="5dp"
            android:layout_weight="0.11"
            android:paddingEnd="@android:dimen/app_icon_size"
            android:scaleType="centerCrop"
            />


        <TextView
            android:text="@{viewHolder.spot.name}"
            android:id="@+id/spotname"
            android:padding="15dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.AppCompat.Title"
            android:textColor="@android:color/background_dark"
            android:textStyle="bold"
            />

        <TextView
            android:text="@{viewHolder.spot.gps}"
            android:id="@+id/gps"
            android:layout_below="@+id/spotname"
            android:textSize="18dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textStyle="bold"

            />


    </RelativeLayout>


</layout>